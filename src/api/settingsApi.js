import apiClient, { USE_MOCKS } from './apiClient'
import { mockDelay } from './mockHelpers'

// Signatures are fixed now against the architecture plan's contract; mock
// branches are filled in per-resource as each page is built.
export const settingsApi = {
  // populated in the next build phase
}
